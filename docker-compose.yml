services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      # Hot-reload: mount source so changes reflect without rebuild
      - ./backend:/app
    env_file:
      - .env
    environment:
      - ENVIRONMENT=development
      # Override Supabase URL to use local db container in dev (avoids IPv6 issues)
      - DATABASE_URL=postgresql+asyncpg://triageai:triageai_dev@db:5432/triageai
      - PYTHONPATH=/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:15-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: triageai
      POSTGRES_PASSWORD: triageai_dev
      POSTGRES_DB: triageai
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U triageai"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data:
